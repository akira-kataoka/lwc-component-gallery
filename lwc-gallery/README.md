# LWC コンポーネント配布ギャラリー

汎用 Lightning Web Component（LWC）を **閲覧・コピー・ダウンロード** できるスタンドアロンの配布ページです。
ブラウザで [index.html](index.html) を開くだけで動作し、Salesforce 環境やビルドツールは不要です（GitHub Pages 等でそのまま公開可）。

## 何ができる？

各コンポーネントについて以下を提供します。

- **ライブプレビュー** — 実 LWC の CSS を使った見た目・動作の再現（クリックやソートも動く）
- **プロパティ / イベント / スロット** の一覧（API リファレンス）
- **使い方テンプレート** のスニペット
- **ソース表示** — HTML / JS / CSS / meta をタブ切替で表示、ボタンでコピー & ダウンロード

## 収録コンポーネント（123種）

> プレビューは各コンポーネント上部のボタンで **PC / タブレット / スマホ** の幅に切替えて表示確認できます。


左サイドバーはカテゴリごとに折りたためるため、種類が増えても氾濫しません。
各コンポーネントは **日本語名（English）** で併記しています。

- **表示**: バッジ / データテーブル / プログレスバー / アバター / ピル / スター評価 / KPI表示 / タイムライン / ステータスドット / リスト / メーター / カウントバッジ / タグ / キーボードキー / リボン / カレンダー / カルーセル / アバターグループ / コードブロック / スパークライン / 棒グラフ / 評価サマリー / 定義リスト / ゲージ / トレンドバッジ / サーモメーター / 開閉テキスト / タグクラウド / カウントアップ / チャットバブル / 連絡先カード / 料金カード / 添付ファイル / ヒートマップ / 数値フォーマット / マーキー / 時計 / 相対時刻
- **アクション**: ボタン / ボタングループ / アイコンボタン / コピーボタン / 分割ボタン / フローティングボタン / トグルボタン / 処理中ボタン / スピードダイヤル
- **レイアウト**: カード / アコーディオン / 区切り線 / ツールバー / メディアオブジェクト / グリッド / 折りたたみセクション
- **オーバーレイ**: モーダル / ツールチップ / ポップオーバー / ドロワー / 確認ダイアログ
- **ナビゲーション**: タブ / ページネーション / パンくずリスト / ステップ / ドロップダウンメニュー / 縦ナビ / ツリー / 縦ステップ / ドットページャー / 縦タブ / ステップウィザード
- **フォーム**: 入力フィールド / トグルスイッチ / セレクト / チェックボックス / テキストエリア / ラジオボタン / 検索ボックス / スライダー / 数値ステッパー / ファイルアップロード / カラースウォッチ / 日付フィールド / タグ入力 / 選択チップ / インライン編集 / OTP入力 / 通貨入力 / 電話番号入力 / リッカート尺度 / パスワード入力 / コンボボックス / ピル複数選択 / タイル選択 / カラー入力 / 時刻入力 / フィルターチップ / 日付範囲 / コピー入力
- **フィードバック**: アラート / スピナー / トースト通知 / 空状態 / 円形プログレス / スケルトン / バナー / セグメント進捗 / コールアウト / ローディングドット / スナックバー / 多段プログレス
- **ダッシュボード**: 指標カード / ドーナツチャート / アクティビティフィード / 目標プログレス / ブレットチャート / ランキング
- **レコード**（Recordページ向け・SF実データ連携）: レコードカード（getRecord） / レコード表示（record-view-form） / レコード編集（record-edit-form） / レコードパス / 関連リスト / ルックアップ / ハイライトパネル

実体は [`../force-app/main/default/lwc`](../force-app/main/default/lwc) 配下の本物の LWC です。

## ファイル構成

```
lwc-gallery/
├── index.html        … 配布ページ本体
├── styles.css        … ページのレイアウト/UI
├── app.js            … ナビ・ライブデモ・ソースビューア・コピー/DL
├── build.mjs         … 実 LWC からデータを生成するスクリプト
├── data.js           … 【自動生成】各 LWC のメタ情報 + ソース
└── components.css    … 【自動生成】全 LWC の CSS 連結（デモ用）
```

`data.js` と `components.css` は **自動生成** です。直接編集しないでください。

## 再生成（LWC を更新したら）

LWC 本体を編集したら、ソース表示とデモを同期するために再ビルドします。

```bash
cd lwc-gallery
node build.mjs
```

`build.mjs` が `force-app/main/default/lwc` を読み取り、`data.js` と `components.css` を作り直します。
これによりカタログは常に実 LWC と一致し、ソースを二重管理しません。

## Salesforce へのデプロイ

各コンポーネントは `lightning__AppPage` / `lightning__RecordPage` / `lightning__HomePage` に公開済み
（`isExposed=true`）。Lightning アプリビルダーからページに配置できます。

```bash
# 例: 全 LWC をデプロイ
sf project deploy start -d force-app/main/default/lwc
```

## 注意

- プレビューは実 LWC の CSS を用いた **再現** です。`lightning-icon` はギャラリー側では省略しています
  （アイコンは Salesforce 上でのみ描画）。
- ページは静的なため、`file://` で直接開いてもクリップボードコピー以外は動作します。
